# Python
__pycache__/
*.pyc
*.pyo

# Experiment outputs (regenerable; conclusions live in RESULTS.md)
runs/
runs_*/

# PyPy venv for experiment 5, rebuilt by experiments/experiment_5/setup_pypy.py
.venv-pypy/

# Throwaway scripts. Anything that backs a reported number must be moved out of
# scratch_ into a named, tracked file first.
scratch_*

# Console logs from ad-hoc runs
*_console.log

# Local-only material: downloaded papers (copyrighted), personal notes, editor and
# assistant settings, the abandoned LNDP code base
papers/
add_to_latex.md
CLAUDE.md
.claude/
LNDP/
